// Updated script to use pre-downloaded DeepSeek-R1 models

const fs = require('fs');
const path = require('path');

async function useDeepSeekR1Local() {
  try {
    console.log('Loading DeepSeek-R1 model from local directory...');

    // Specify the path to the pre-downloaded model
    const modelPath = path.join(__dirname, '../DeepSeek-R1/models');

    if (!fs.existsSync(modelPath)) {
      throw new Error('Model directory not found. Please download the models manually.');
    }

    // Example input for reasoning
    const input = 'Solve the math problem: What is 12 * 8?';

    console.log('Generating response using local model...');
    // Simulate model interaction (replace with actual model logic)
    const output = `Response generated by DeepSeek-R1 for input: "${input}"`;

    console.log('Response:', output);
  } catch (error) {
    console.error('Error using DeepSeek-R1:', error);
  }
}

useDeepSeekR1Local();
